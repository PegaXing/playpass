# PlayPass

## Setup on Mac — run these 4 commands in Terminal

```
cd ~/Downloads/playpass/apps/mobile
npm install
npx expo prebuild --platform ios --clean
open ios/PlayPass.xcworkspace
```

Then in Xcode: plug in iPhone → select it → press Run (Cmd+R)
